<html>
<head>
  <style>
      h1{
      margin-top:50px;
    }
    body {
            height: 100vh;
            display: flex;
            align-items: center;
             background: linear-gradient(#141e30, #243b55);
            overflow:hidden;
        }
       
        .container{
          width:500px;
          box-shadow: 10px 10px 5px lightblue;
          border-radius: 10px;
            background: white;
            padding: 5px 10px;
        }      
  </style>
  </head>
      <body>
          <div class="container">
            <div class="row justify-content-md-center"> 
           
                <div class="projects">
<p style="color: green"><%= notice %></p>

<%= render @prmer %>

<div>
  <%= link_to "Edit", edit_prmer_path(@prmer), class: "btn btn-primary my-2" %> |
  <%= link_to "Back", prm_path(@prmer.prm), class: "btn btn-warning my-2" %>

  <%= button_to "Destroy", @prmer, method: :delete, class: "btn btn-danger" %>
</div>
  </div>
</html>   
