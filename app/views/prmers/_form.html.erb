<style>
.form-control {
  width: 400px;
}
</style>
<%= form_with(model: prmer) do |form| %>
  <% if prmer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prmer.errors.count, "error") %> prohibited this prmer from being saved:</h2>

      <ul>
        <% prmer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= form.text_field :prm_id, type: :hidden %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary my-2" %>
  </div>
<% end %>
